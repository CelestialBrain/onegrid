node_modules
dist
build
coverage
.turbo
.next
.nuxt
.svelte-kit
.solid
out

*.log
.DS_Store
.env
.env.local
.env.*.local

*.tsbuildinfo
.vite

# Playwright/bench outputs — churn every run, not source-of-truth.
playwright-report/
test-results/

.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea

# Claude Code runtime state.
.claude/

# Local-only competitor probe artifacts (restored from commit 99c7e4d^).
# Reverse-engineered library docs used for gap planning + roadmap input.
# Per the clean-room MIT discipline these files MUST NOT land in the
# published tree — they reference specific commercial grid libraries
# by name. Keep here for local planning; restore via `scripts/restore-research.sh`.
.research/
