## API Report File for "@onegrid/headless"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import { ColumnDef } from '@onegrid/core';
import { FilterModel } from '@onegrid/protocol';
import { Grid } from '@onegrid/core';
import { GridOptions } from '@onegrid/core';
import { NestedGridOptions } from '@onegrid/core';
import { RowSource } from '@onegrid/core';
import { SelectionSnapshot } from '@onegrid/core';
import { SortModel } from '@onegrid/protocol';

// @public (undocumented)
export function createHeadlessGrid(config: HeadlessGridConfig): HeadlessGrid;

// @public (undocumented)
export type HeadlessEvent = 'selectionChange' | 'sortChange' | 'filterChange' | 'columnsChange' | 'scroll' | 'frame' | 'invalidate' | 'mount' | 'unmount';

// @public (undocumented)
export interface HeadlessEventPayload {
    // (undocumented)
    columnsChange: ReadonlyArray<ColumnDef>;
    // (undocumented)
    filterChange: FilterModel | undefined;
    // (undocumented)
    frame: {
        fps: number;
    };
    // (undocumented)
    invalidate: {
        reason: string;
    };
    // (undocumented)
    mount: void;
    // (undocumented)
    scroll: {
        scrollTop: number;
        scrollLeft: number;
    };
    // (undocumented)
    selectionChange: SelectionSnapshot;
    // (undocumented)
    sortChange: SortModel;
    // (undocumented)
    unmount: void;
}

// @public
export class HeadlessGrid {
    constructor(config: HeadlessGridConfig, schedulers?: {
        readonly raf?: (cb: () => void) => number;
        readonly cancel?: (h: number) => void;
    });
    get core(): Grid | null;
    // (undocumented)
    getColumns(): ReadonlyArray<ColumnDef> | undefined;
    // (undocumented)
    getSelection(): SelectionSnapshot | undefined;
    hostConnected(): void;
    hostDisconnected(): void;
    hostUpdate(): void;
    hostUpdated(): void;
    invalidate(reason?: string): void;
    renderAccessibilityShadowHTML(): string;
    requestUpdate(reason?: string): void;
    // (undocumented)
    scrollToRow(rowIndex: number): void;
    // (undocumented)
    setColumns(columns: ReadonlyArray<ColumnDef>): void;
    // (undocumented)
    setFilter(filter: FilterModel | undefined): void;
    // (undocumented)
    setRowSource(rowSource: RowSource, rowHeight: number | Float32Array): void;
    // (undocumented)
    setSort(sort: SortModel): void;
    // Warning: (ae-forgotten-export) The symbol "Listener" needs to be exported by the entry point index.d.ts
    subscribe<E extends HeadlessEvent>(event: E, listener: Listener<E>): () => void;
}

// @public (undocumented)
export interface HeadlessGridConfig {
    readonly hydrateFrom?: HTMLElement;
    readonly options: GridOptions | NestedGridOptions;
}

// (No @packageDocumentation comment for this package)

```
