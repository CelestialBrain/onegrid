{
  "name": "@onegrid/duckdb-join",
  "version": "1.0.0",
  "description": "Cross-database joins via DuckDB-WASM. Register heterogeneous data sources (Postgres, MySQL, ClickHouse, Mongo, Arrow files) as DuckDB views; run a single SQL query that joins across them inside the browser in the same render frame.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.cjs",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "require": "./dist/index.cjs"
    }
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "clean": "rm -rf dist .turbo *.tsbuildinfo",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src"
  },
  "dependencies": {
    "@onegrid/duckdb": "workspace:*",
    "@onegrid/protocol": "workspace:*"
  },
  "peerDependencies": {
    "@duckdb/duckdb-wasm": "^1.28.0"
  },
  "peerDependenciesMeta": {
    "@duckdb/duckdb-wasm": {
      "optional": true
    }
  },
  "devDependencies": {
    "@duckdb/duckdb-wasm": "^1.28.0",
    "fast-check": "^4.8.0"
  },
  "publishConfig": {
    "access": "public"
  },
  "sideEffects": false
}
